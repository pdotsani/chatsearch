# Chat Search

## Setup

In the source directory, activate the local environment:

`activate`

Then run:

`pip install -r requirements.txt`
`ollama serve`
`ollama pull gemma3`

To run locally:

`flask --app app run`
